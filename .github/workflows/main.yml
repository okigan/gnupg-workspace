name: CI

on: [push,pull_request,workflow_dispatch]

jobs:
  build:
    strategy:
      matrix:
        runs-on: [ubuntu-latest]

    runs-on: ${{ matrix.runs-on }}

    steps:
      - name: step1
        run: |
          ./install.sh
          ./build.sh
